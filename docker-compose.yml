version: '2'

services:
  web:
    build: .
    image: phpscrapyapp
    depends_on:
      - "db"
    ports:
      - "80:80"

  db:
    image: mongo
    container_name: some-mongo
    ports:
      - "27017:27017"
